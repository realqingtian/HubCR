# HubCR 仓库指令

[English](AGENTS.md) | **简体中文**

这是 HubCR 仓库的主要指令入口，只包含项目所有区域共同遵守的仓库级规则。

## 指令作用域与继承

- 本文件作用于整个仓库。
- `backend/AGENTS.md` 只为 `backend/` 下的文件增加规则。
- `frontend/AGENTS.md` 只为 `frontend/` 下的文件增加规则。
- 子目录指令会补充本文件；当规则更具体时，只在自己的目录范围内优先。
- 后端专属规则不能用于约束前端，前端专属规则也不能用于约束后端。
- 如果任务同时涉及前后端，应同时读取两个子目录指令，但每条规则仍限制在声明的
  作用域内。
- `backend/` 与 `frontend/` 之外的文件只遵守本文件及离它更近的指令文件。

## 开始任务前必须执行

- 阅读 `README.md`，了解当前功能与限制。
- 结构或行为变更前，阅读 `docs/architecture.md` 和 `docs/development.md`。
- 规划或实现产品能力前，阅读 `docs/requirements.md` 和
  `docs/development-plan.md`。必须遵守其中的决策门，不得自行补充缺失策略来启动
  被阻塞的工作包。
- 阅读离每个待修改文件最近的 `AGENTS.md`。
- 编辑前检查 `git status` 和相关实现，保留无关用户修改，不得丢弃不是自己创建的
  工作。
- 以当前代码和配置为准，不得把路线图中的能力描述为已经实现。

## 仓库级架构

- HubCR 在 MVP 阶段采用模块化单体。
- Go 提供业务控制面和异步 Worker。
- CNCF Distribution 提供 OCI 数据面，并负责 `/v2/`、Manifest、Blob、上传、
  下载和存储驱动行为。
- PostgreSQL 存储持久化业务状态，并在初期承载异步任务。
- Redis 用于缓存、限流状态和短期协调。
- OCI 内容通过 Distribution 存储到 S3 兼容存储；本地开发使用 MinIO。
- 耗时的扫描和验签工作位于同步请求与 Push 主链路之外。
- Web 应用是控制面 API 的独立消费者，不拥有后端授权或持久化规则。
- 修改这些架构选择前，必须记录决定并获得维护者确认。

## 共享产品与安全事实

- 命名空间路径采用 `hubcr.io/{namespace}/{repository}:{tag}`。
- 仓库可见性明确为 `PUBLIC` 或 `PRIVATE`；数据缺失或不可用时不能静默变为公开。
- 即使内容按 Digest 物理去重，每次仓库访问仍必须执行授权判断。
- Web 会话不能作为长期 Registry 凭据。
- Registry Token 必须是短期令牌，并限制到精确的仓库和 `pull`、`push`、
  `delete` 等允许操作。
- Artifact 以不可变 Digest 标识，Tag 是可变引用。
- 扫描报告、SBOM、签名与验签结果绑定 Artifact Digest。
- 在 API、存储和 UI 中，签名存在、密码学有效与策略可信必须是不同状态。
- 除非已接受的策略明确要求阻断，否则扫描和验签保持异步。
- 禁止暴露、记录或提交真实凭据、Token、Authorization Header 或敏感个人数据。

## 尚未确认的产品决策

不得静默决定注册模式、组织角色名称、仓库授权继承、基于扫描的 Pull 阻断、签名
信任根、生产部署目标、计费、配额、保留周期或内容治理。实现依赖这些决策的
Schema、API 或 UI 契约前，必须记录决定并获得确认。

## 共享实现流程

- 改动聚焦于用户要求的行为，避免无关重构。
- 添加代码前确认所属区域与模块。
- 规划内的产品工作应引用相关需求与工作包 ID。只有验收证据发生变化时才能更新
  计划状态。
- 行为变化与回归必须增加或更新聚焦测试。
- 优先使用现有依赖，新增生产依赖前必须解释原因。
- 命令、边界或可用行为变化时，更新架构与状态文档。
- 开发中运行针对性检查，完成前运行仓库级门禁。
- 明确报告实际验证内容和所有尚未测试的运行时或外部服务路径。

## 文档与本地化

- 英文是项目文档的权威语言。
- 每份面向用户的 Markdown 文档都有同步的 `.zh-CN.md` 版本，并在顶部提供语言
  切换。
- 同一次改动同步更新两种语言。翻译中保持代码标识符、路径、命令、变量、API
  示例和协议名称不变。
- 不得把规划中的行为写成已经可用。
- AI 适配文件应导入或指向离它最近的 `AGENTS.md`，不能复制成独立维护的规则。

## 仓库级验证

宣布完成前运行：

```bash
make check
```

文档改动还要验证相对 Markdown 链接、双向语言跳转、尾随空格和文件末尾换行。
行为变化应在可行时执行运行时冒烟测试。如果 Docker 或其他依赖不可用，应说明
限制，不能声称对应路径已经测试。

## Git 与外部操作

- 除非用户明确要求，否则不得提交、推送、强制推送、打 Tag、发布、创建 PR 或
  修改外部系统。
- 每次完成并验证开发或修改任务后，必须主动询问用户是否需要将改动提交并推送到
  已配置的远程仓库。询问不代表已经获得授权；必须等待用户明确确认后才能暂存、
  提交或推送。
- 禁止使用破坏性 Git 命令删除不是自己创建的改动。
- 获得提交授权后，提交前检查暂存差异、运行 `git diff --cached --check` 并扫描
  暂存文件中的凭据。

## 仓库级代码审查规则

- 标记违反控制面与数据面边界的改动。
- 标记可以绕过命名空间或仓库检查的授权路径。
- 标记仅按 Tag 而不是 Digest 保存的扫描、签名或可信结果。
- 标记未经已接受策略就加入 Push 主链路的同步安全任务。
- 标记把规划功能写成或显示为已实现的改动。
- 标记只修改英文文档却未同步简体中文，或反过来的情况。
